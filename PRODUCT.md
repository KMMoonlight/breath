# Product

## Register

product

## Users

Breath 面向在本地 macOS 开发环境中同时组织多个编码 Agent、终端会话和 Git 工作流的软件开发者。用户处在持续操作与审阅状态，需要高信息密度、低干扰、可预测的专业工具界面。

## Product Purpose

Breath 是本地优先的多 Agent 工作台。它把工作区、工作会话、终端和通用 Git 工作台组织在同一个原生 macOS 应用中，同时保持 Git 工具独立于 Agent 上下文。成功意味着用户能快速理解当前状态、精确执行操作，并信任工具不会隐式修改项目或隐藏风险。

## Brand Personality

克制、专业、可靠。界面应像成熟开发工具一样服务于任务，保持原生 macOS 质感，并在 Git 工作流上采用 JetBrains 用户熟悉的信息架构和术语。

## Anti-references

- 与应用主题脱节的系统级浅色弹窗或一次性自定义控件。
- 大图标、大标题、玻璃卡片和装饰性动效主导的消费级界面。
- 机械复制 JetBrains 像素外观，或使用 WebView、Electron 风格组件替代原生 macOS 控件。
- 只依赖颜色表达状态，或让危险操作缺少明确影响说明。

## Design Principles

- 熟悉优先：标准操作采用用户已经理解的 macOS 与开发工具惯例。
- 工具退后：用紧凑层级和低噪声状态让代码、Diff 与操作对象成为主角。
- 一致即反馈：相同行为共享同一组件、主题、间距和交互状态。
- 风险透明：破坏性操作显示对象、范围和后果，不做不可见回滚。
- 本地可信：保持用户文件、Git 和 Agent 工具的所有权边界清晰。

## Accessibility & Inclusion

所有能力需要支持键盘操作、VoiceOver 和清晰焦点状态；状态不能只依赖颜色，应同时提供图标或文字。尊重系统深浅外观、减少动态效果设置和原生可访问性语义。
