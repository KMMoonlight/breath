# 将 Skill 直接安装到各 Agent 目录

Breath 不在自身应用数据目录保存全局 Skill 的规范副本，也不通过符号链接向 Agent 分发。每个被用户选中的 Agent CLI 都在自己的全局 Skill 目录中获得一份完整、独立的数据文件，使 Agent 目录始终是实际安装状态的事实来源，且移除 Breath 不会破坏 Skill；不同目标中的副本之后可以独立变化，Breath 按实际内容扫描和展示。外部工具创建的符号链接仍可被扫描；卸载只删除所选 Agent 的链接，只有用户明确更新时才把该链接替换为所选 Agent 自己的新版副本。
