# 用兼容矩阵限定 Agent CLI 支持范围

Breath 不承诺自动支持所有现在或未来提供 hooks 的 Agent CLI，而是维护带最低兼容版本和适配器测试的明确支持矩阵。首版矩阵包含 Codex、Claude Code、Gemini CLI、GitHub Copilot CLI、Qwen Code、Cursor Agent、Factory Droid、OpenCode 和 Pi；其他 CLI 仍可在通用终端中运行，只有具备官方、稳定、本地生命周期扩展机制并通过兼容性验证后才进入矩阵。
